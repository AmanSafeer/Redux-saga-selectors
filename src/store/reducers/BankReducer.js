import {
   
} from '../constants';

const initialState = {
    
}

export default function BankReducer(state = JSON.parse(JSON.stringify(initialState)), action) {
    switch (action.type) {

        default:
            break;
    }

    return state;
}