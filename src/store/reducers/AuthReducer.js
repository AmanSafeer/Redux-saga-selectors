import {
   
} from '../constants';

const initialState = {
    user: {},
    error: {},
    loader: false,
}

export default function AuthReducer(state = initialState, action) {
    switch (action.type) {
        default:
            break;
    }

    return state;
}